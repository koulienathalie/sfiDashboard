# =============================================
# Frontend Environment Configuration
# =============================================
# 
# Ces variables sont lues automatiquement par Vite lors du démarrage
# Les fallbacks sont intégrés dans le code frontend
#
# Accessible sur: http://localhost:5173
# =============================================

# API Backend URL avec fallback automatique
# Si cette adresse n'est pas accessible, le code utilise http://localhost:3001
VITE_API_URL=http://localhost:3001

# WebSocket Backend URL avec fallback automatique
# Si cette adresse n'est pas accessible, le code utilise ws://localhost:3001
VITE_BACKEND_WS_URL=ws://localhost:3001

# Mode de développement
VITE_ENV=development
